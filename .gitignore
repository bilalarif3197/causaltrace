# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
.next/
out/
*.tsbuildinfo

# Env
.env
.env.local
.env*.local

# OS
.DS_Store

# Evaluation output
evaluation/results/

# Harvested PMC content. Article text is third-party and the OA subset includes
# NC/ND licences, so cached full text and narratives are never committed.
evaluation/.pmc_cache/
evaluation/pmc_candidates.json
backend/causaltrace.db
backend/causaltrace.db-wal
backend/causaltrace.db-shm
tools/shots/

# Challenge materials from Regeneron / HackMIT. Third-party content we do not
# own, so it is not redistributable under this repo's MIT license. Kept on
# disk for reference, never committed.
*.docx
*.pdf

# Cached openFDA label responses.
backend/.openfda_cache/

# LiverTox request cache. The harvested cases themselves ARE committed:
# LiverTox text is not copyright protected, unlike the PMC NC/ND articles.
evaluation/.livertox_cache/
